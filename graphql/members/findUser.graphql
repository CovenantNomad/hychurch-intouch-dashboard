query findUser($id: ID!) {
  user(id: $id) {
    id
    name
    phone
    isActive
    birthday
    registrationDate
    gender
    address
    description
    roles
    cell {
      id
      name
    }
  }
}
