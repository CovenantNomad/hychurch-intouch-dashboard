query findCellLists($limit: Int) {
  findCells(limit: $limit) {
    nodes {
      id
      name
      community
      leaders {
        id
        name
        birthday
        roles
      }
    }
  }
}
